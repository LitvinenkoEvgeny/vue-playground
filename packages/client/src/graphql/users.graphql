query GetUsers($page: Int!) {
    users(page: $page){
        page
        total_pages
        users {
            id
            first_name
            email
            last_name
            image: avatar
        }
    }
}
